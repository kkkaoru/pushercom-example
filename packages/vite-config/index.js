const plugins = require('./plugins/typescript');
const reactPlugins = require('./plugins/typescript-react');
const rollupOptions = require('./rollup-options/react');

module.exports = {
  plugins,
  reactPlugins,
  rollupOptions,
};
